name: Basecamp
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  # firebase_core: ^4.2.1
  google_generative_ai: ^0.4.0
  flutter_dotenv: ^5.1.0  # For API key management (optional but recommended)
  shared_preferences: ^2.2.0  # For persisting chat messages
  file_selector: ^1.0.4
  supabase_flutter: ^2.5.0
  # firebase_auth: ^6.1.2
  url_launcher: ^6.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  intl: ^0.19.0
  table_calendar: ^3.0.9 
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
